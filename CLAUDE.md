# draft-review 草稿审稿站 · 写作规定

## 这个站是干什么的

- 这是叶扬博客（Gmeek，https://yeyangchen2009.github.io ）的**草稿审稿站**，docsify 在浏览器现场渲染，主要给手机端躺着审稿用。
- 这里产出的每一篇，**最终目的地是 Gmeek 博客的 issue**，不是公众号。一切写法以「搬上博客之后读着对味」为唯一标准。
- 首页 README.md 与 _sidebar.md 由 `scripts/sync_review_site.py` 自动生成，**不要手动改**。

---

## 一、人称（最高优先级，也是最容易犯的一条）

- 全文第三人称叙事，主角叫**叶扬**。
- **禁止用「他」回指叶扬。** 同一句或相邻几句再次提到叶扬，要么再写一次「叶扬」，要么直接省略主语——中文无主句天然成立，读着还更干净。
- 正例反例：
  - ❌ 叶扬想搞清楚，他先翻了一遍抽屉，他发现数据比想象中多。
  - ✅ 叶扬想搞清楚，先翻了一遍抽屉，发现数据比想象中多。
  - ✅ 叶扬想搞清楚这件事。叶扬先翻了抽屉，结果有点意外。
- 写到家人、项目作者等第三方时可以正常用「他」，但能写名字或身份就写，别让人在一串「他」里找人。
- **定稿前固定动作：全文搜一遍「他」，逐个确认没有一个是指叶扬的。**

## 二、人设

- 叶扬是**程序员**，日常写前后端和数据库，也做过多年大数据。所有类比从这个世界取材：调用链、数据库、事务、日志、CI、编译、缓存、接口、进程、快照……
- **禁止「非程序员／纯小白」人设**，不要把叶扬写成对技术一窍不通的普通人。
- 可以默认读者未必懂技术，但每个程序员类比后面**带一句白话注释**把人接上——类比负责搭桥，白话负责兜底，别让读者卡在术语上。

## 三、文风

- 口语、亲切、带点幽默和个人脾气，像一个懂行的朋友在讲事，不像产品文档，更不像爆款营销稿。
- **破折号 —— 正常用、鼓励用**，用来追加补充和转折。本目录明确推翻上层「禁止破折号」那条规则。
- 句子长短错落，别全靠逗号一路拖到底，该断就断。
- 这些「叶扬式动作」可以保留，但要自然，别每篇机械照抄：打完比方后「照例戳破这个类比的洞」；出问题叫「翻车」「血案」；教程收尾放「翻车急诊室」「完工验收清单」。

## 四、结构与去痕

- **正文第一行不要写一级标题（H1）。** 博客的标题走 issue 标题，正文再写一个 H1 会重复。
- frontmatter 从简，保留 `title / date / status / tags` 即可。`framework`、`opening_type`、`source_url` 这些是写作时给自己看的八股，**定稿前删掉**，别把脚手架留在成品里。
- 文末「备选标题」「以下内容发布前删除」那一整块，定稿前删除。
- 小标题说朴素的人话，不要震惊体、标题党。

## 五、真实性（博客的生命线）

- 项目名、star 数、版本号、命令、引文，**必须真实、可复核**。拿不准的当场查证，查不到就删掉或标「待核实」，**绝不许编**。
- 优先写「自己真跑过、真截图」的内容。调研类文章是地图不是成稿，行文中要分清哪些是已实测、哪些只是据项目文档。
- 数字给得保守：写「约 0.95 万 star」比写精确的「9523」更稳，除非刚查过。

## 六、Mermaid

- 示意图**一律用 flowchart**（优先 TD），不用 sequenceDiagram——时序图的信号文字在亮色页面上没有底色、几乎不可读。
- 每个 mermaid 块第一行加暗色 init（GitHub Dark 配色，`clusterBkg`／`clusterBorder` 不能省）：

```
%%{init: {'theme':'dark','themeVariables':{'background':'#0d1117','primaryColor':'#1f6feb','primaryTextColor':'#ffffff','primaryBorderColor':'#79c0ff','secondaryColor':'#21262d','tertiaryColor':'#30363d','lineColor':'#8b949e','textColor':'#e6edf3','clusterBkg':'#161b22','clusterBorder':'#58a6ff','edgeLabelBackground':'#21262d','fontSize':'15px'}}}%%
```

- 审稿站 index.html 会剥掉 init、按手机端明暗重新渲染，所以源稿保留 init 就行，不用迁就审稿站。

## 七、版权红线

- 只发公共知识和工程方法。**不发任何课程字幕、讲稿原文**；公开发布物里不出现「讲稿」二字，提到课程内容写「课程里」。
- 第三方题库、素材只讲自己怎么用，不搬运、不分发受版权保护的内容。

## 八、文件与同步

- 草稿文件名：`YYYY年M月D日-标题关键词.md`。
- **系列分组**：长篇系列在草稿源目录（`vault/07.发布文案/2026年/草稿/`）下建一级子目录，脚本自动分组、该组在 sidebar 排前。已登记：`中系/`＝中国佛教史。开新长系列时在 `sync_review_site.py` 的 `GROUP_TITLES`／`GROUP_ORDER` 里登记；顶层散稿自动归入默认组。
- 写完保存后执行 `python F:\Data\AI-obsidian\scripts\sync_review_site.py` 同步到手机；只想本地预览时加 `--no-push`。
- 审稿站是 **public 仓库**，草稿即公开的半成品（docsify 空壳渲染，基本不被搜索引擎收录）。介意提前曝光的稿只加 `--no-push` 本地预览，不推。

## 九、两阶段工作流（中系及所有重头系列）

一篇文章只在 Gmeek 构建一次——写作迭代全部留在审稿站，不占用 Actions 流水线。

**阶段一 · 审稿站快循环（不碰 Gmeek）**

1. 在草稿源目录（系列放子目录）写稿，frontmatter 从简；
2. `sync_review_site.py` 推送，手机/浏览器即时审稿，改一版推一次，秒级生效；
3. fork 独立审直接在 md 阶段完成（0 P0 才放行），所有结构性修改在这一阶段做完；
4. 人称、mermaid init、脚手架去痕按本文件各节在 md 里处理干净。

**阶段二 · Gmeek 一次性发布（每篇只构建这一次）**

1. 搬稿转换：删 frontmatter 整块；正文第一行仍不许是 H1；配图路径改成 `/screenshots/xxx.png`；内链用 `/post/N.html`；
2. 配图卡片、mermaid 实拍按各系列既有规矩出图；有新 static 文件先全量重建；
3. `gh issue create`（标签按系列）→ 等 Generate/Deploy 双绿；
4. **`gh api markdown` 整稿终验不能省**：docsify 用 marked 渲染，GFM 中文粗体坑在审稿站看不出来，字面 `**` 必须为 0、strong 开闭数相等；
5. CDP 线上验收（注入 primer、mermaid 轮询）→ 回系列总目打勾（中系回 #72）。

**发布后草稿的归宿**：vault 源稿保留，`status` 改「已发布」、顶部加博客链接，审稿站即存档。此后修订一律改 Gmeek issue，不许回头改审稿站稿，避免两份正文分叉。

---

## 规则优先级

本目录 CLAUDE.md 与上层 `F:\Data\AI-obsidian\AGENTS.md`、`vault/CLAUDE.md` 冲突时，**一律以本目录为准**——尤其是这三条：这里写的是博客、不是公众号；破折号可以用；人称不用「他」回指叶扬。
